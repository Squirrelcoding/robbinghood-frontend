"use client";
import React from "react";

// Placeholder MonthlySalesChart — the original chart code was removed to
// eliminate unused-import lint errors. Reintroduce the chart rendering
// (ReactApexChart, options, series) if you need the chart in the UI.
export default function MonthlySalesChart() {
  return <></>;
}

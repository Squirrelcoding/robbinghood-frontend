"use client";
import React from "react";

// Simplified placeholder for MonthlyTarget; remove unused imports and
// chart config to satisfy linting. Re-add the chart code when needed.
export default function MonthlyTarget() {
  return <div className="rounded-2xl border border-gray-200 bg-gray-100 dark:border-gray-800 dark:bg-white/[0.03]" />;
}
